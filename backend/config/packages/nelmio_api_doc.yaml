nelmio_api_doc:
  documentation:
    info:
      title: Sportgram API
      description: API documentation for Sportgram
      version: 1.0.0
    components:
      securitySchemes:
        Bearer:
          type: http
          scheme: bearer
          bearerFormat: JWT

  areas:
    default:
      path_patterns:
        - ^/

  models:
    use_jms: false
    names:
      - { alias: UserResource, type: App\Resource\UserResource }